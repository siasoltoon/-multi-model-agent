[build]
builder = "DOCKERFILE"

deploy.healthcheckPath = "/health"
deploy.healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[deploy]
startCommand = "uvicorn agent_platform.app:app --host 0.0.0.0 --port $PORT"
